{
  "id": "global-gitignore",
  "version": "1.0.0",
  "name": "Global Gitignore",
  "description": "Mounts the host's ~/.gitignore into the container and configures it as the git global excludes file.",
  "mounts": [
    {
      "type": "bind",
      "source": "${localEnv:HOME}/.gitignore",
      "target": "/home/vscode/.gitignore"
    }
  ],
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
